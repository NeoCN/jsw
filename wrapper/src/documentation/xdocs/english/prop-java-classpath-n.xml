<?xml version="1.0"?>

<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML V4.1.2//EN" "../dtd/docbook/4.1.2/docbookx.dtd">

<!-- ********************************************************************
     $Id$
     ******************************************************************** -->

<chapter id="prop-java-classpath-n">
    <title>wrapper.java.classpath.&lt;n&gt; Property</title>
    <section>
        <title>Configuration Property Overview</title>
        <itemizedlist>
            <listitem>
                <ulink url="properties.html">Configuration Property Overview</ulink>
            </listitem>
            <listitem>
                <ulink url="props-jvm.html">JVM Configuration Properties</ulink>
            </listitem>
            <listitem>
                <ulink url="properties.html#name">Property List by Name</ulink>
            </listitem>
        </itemizedlist>
    </section>
    <section>
        <title>wrapper.java.classpath.&lt;n&gt;</title>
        <para>
            Java Classpath to use.  You should have a series of properties
            listing up the various class path elements to use when launching
            the application.  Each element has a property name which starts
            with <literal moreinfo="none">wrapper.java.classpath.</literal>
            and ends with an integer number counting up from 1.  There can
            be no missing numbers.
        </para>
        <para>
            This list must contain the
            <literal moreinfo="none">wrapper.jar</literal> file.  It can
            contain jar files as well as directories containing class files.
        </para>
        <informalexample>
            <simpara><emphasis>Example:</emphasis></simpara>
            <screen format="linespecific"><![CDATA[wrapper.java.classpath.1=../lib/wrapper.jar
wrapper.java.classpath.2=../lib/myapp.jar
wrapper.java.classpath.3=../lib/mysql.jar
wrapper.java.classpath.4=../classes]]></screen>
        </informalexample>
        <para>
            If a wildcard character is used within a classpath entry, all
            matching files will added to the classpath used when launching
            a JVM instance.  Valid wildcard characters are
            <literal moreinfo="none">'*'</literal> and
            <literal moreinfo="none">'?'</literal>.
            <literal moreinfo="none">'*'</literal> will match 0 or more
            characters and <literal moreinfo="none">'?'</literal> will match
            exactly one character.
        </para>
        <informalexample>
            <simpara><emphasis>Example:</emphasis></simpara>
            <screen format="linespecific"><![CDATA[wrapper.java.classpath.1=../lib/*.jar
wrapper.java.classpath.2=../classes]]></screen>
        </informalexample>
    </section>
</chapter>
